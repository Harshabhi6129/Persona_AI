openai
streamlit
chromadb
numpy
pysqlite3-binary  # Fixes SQLite compatibility (only if needed)
tiktoken  # Needed for OpenAI API tokenization
sentence-transformers  # Enhances retrieval efficiency
duckdb  # Alternative backend for ChromaDB (mandatory to avoid SQLite issues)
